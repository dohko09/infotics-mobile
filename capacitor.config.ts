import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.codementor.infotics',
  appName: 'InfoTICS',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
